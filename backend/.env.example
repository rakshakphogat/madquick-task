# Environment Configuration for Madquick Password Manager Backend
# Copy this file to .env and update the values

# Server Configuration
PORT=5000

# MongoDB Database Configuration
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/madquick-password-manager
# For MongoDB Atlas (cloud), use format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name

# JWT Secret for Token Authentication
# Generate a secure random string for production
# You can use: node -p "require('crypto').randomBytes(64).toString('hex')"
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long

# Node Environment
NODE_ENV=development
# Set to 'production' in production environment